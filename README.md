# yaologo

This repository contains Yao's personal logo drafts.

## Current contents

- `logo_v1` to `logo_v6`
- each version currently has one SVG draft and one review HTML file
- `AGENTS.md`, `README.md`, `LICENSE`, `.gitignore`
- `.omo/` and `.opencode/` are local workspace metadata and are ignored

Keep this README and `AGENTS.md` in sync with the actual draft versions in the repo.

---

## 中文

这个仓库现在保存的是 Yao 的个人 logo 草稿。

### 当前内容

- `logo_v1` 到 `logo_v6`
- 每个版本目录当前都有一个 SVG 草稿和一个 review HTML 文件
- `AGENTS.md`、`README.md`、`LICENSE`、`.gitignore`
- `.omo/` 和 `.opencode/` 是本地工作区元数据，已被忽略

后续如果版本有增减，请同步更新本 README 和 `AGENTS.md`。
