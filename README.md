# yaologo

This repository contains the current SVG for Yao's personal logo.

![Yao logo](./logo.png)

## Current contents

- `logo.svg` as the single retained logo asset
- `logo.png` as a rendered preview derived from `logo.svg`
- `AGENTS.md`, `README.md`, `LICENSE`, `.gitignore`
- `.omo/` and `.opencode/` are local workspace metadata and are ignored
